[project]
name = "cholangiohub-ingestion"
version = "0.1.0"
description = "Ingestion Service for CholangioHub"
requires-python = ">=3.13"

dependencies = [
    "cholangiohub-shared",
    "minio>=7.2",
    "fastapi>=0.95",
    "requests>=2.31"
]

[tool.uv]
package = true